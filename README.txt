Le dossier UtilisationService correspond une solution qui contient un projet qui montre comment est utilisé un service web SOAP que l'ont appel (Calcette)



Le dossier ServiceHotels correspond une solution qui contient deux projets correspond aux exercicex 4:
-Un projet qui crée un service web SOAP qui represent plusieurs hotels.
-Un projet Agence qui represente un client qui accéde au service web SOAP de l'hotel en appelant ses deux méthodes principales.
Une interface graphique a été faite pour l'agence(Client), mais pas de base de données, pour la question 3, l'image est enregistrée dans le dossier img du projet ServiceHotel,
L'agence l'affiche dans une interface (WindowsForm) après resérvation.

